import { makeStyles } from "@mui/styles";

const DiscrubButtonStyles = makeStyles(() => ({
  buttonParent: {
    backgroundColor: "#36393f",
  },
  buttonLogo: {
    width: "24px",
    height: "24px",
  },
}));

export default DiscrubButtonStyles;
