import { makeStyles } from "@mui/styles";

const FloatingLogoStyles = makeStyles(() => ({
  box: {
    position: "fixed",
    top: "14px",
    right: "307px",
  },
}));

export default FloatingLogoStyles;
