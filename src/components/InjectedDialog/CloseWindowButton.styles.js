import { makeStyles } from "@mui/styles";

const CloseWindowButtonStyles = makeStyles(() => ({
  boxContainer: {
    position: "fixed",
    top: "45px",
    right: "245px",
  },
}));

export default CloseWindowButtonStyles;
