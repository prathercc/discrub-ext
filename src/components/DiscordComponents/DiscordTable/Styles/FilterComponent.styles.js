import { makeStyles } from "@mui/styles";

const FilterComponentStyles = makeStyles(() => ({
  stack: {
    width: "100%",
  },
  threadSelect: {
    width: "320px !important",
  },
}));

export default FilterComponentStyles;
